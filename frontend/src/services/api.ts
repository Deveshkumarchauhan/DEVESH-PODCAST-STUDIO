const localApi=`${location.protocol}//${location.hostname}:9090`; const API=(import.meta.env.VITE_API_URL||(import.meta.env.PROD?location.origin:localApi)).replace(/\/$/,''); export const api=async(path:string,options:RequestInit={})=>{const token=localStorage.getItem('studio_token');const r=await fetch(API+path,{...options,headers:{'Content-Type':'application/json',...(token?{Authorization:`Bearer ${token}`}:{...options.headers}),...options.headers}});const data=await r.json();if(!r.ok)throw new Error(data.error||'Request failed');return data};export {API};
