import{useState}from'react';import{api}from'../services/api';export function Auth(){const[register,setRegister]=useState(false),[name,setName]=useState(''),[email,setEmail]=useState(''),[password,setPassword]=useState(''),[error,setError]=useState('');async function submit(e:React.FormEvent){e.preventDefault();try{const r=await api('/api/auth/'+(register?'register':'login'),{method:'POST',body:JSON.stringify({name,email,password})});localStorage.setItem('studio_token',r.token);location.href='/director'}catch(e){setError((e as Error).message)}}return <main className="auth"><section className="auth-card"><span className="eyebrow">CREATOR: DEVESH KUMAR CHAUHAN</span><h1>DEVESH<br/>PODCAST STUDIO</h1><p>Professional Multi-Camera Podcast Studio</p><form onSubmit={submit}>{register&&<input placeholder="Your name" value={name} onChange={e=>setName(e.target.value)}/>}<input type="email" placeholder="Email" value={email} onChange={e=>setEmail(e.target.value)}/><input type="password" placeholder="Password (8+ characters)" value={password} onChange={e=>setPassword(e.target.value)}/>{error&&<small className="error">{error}</small>}<button>{register?'CREATE ACCOUNT':'SIGN IN'}</button></form><a onClick={()=>setRegister(!register)}>{register?'Already have an account? Sign in':'New here? Create account'}</a></section></main>}
